*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing config — nie committen
keystore.jks
*.keystore
keystore.properties

# Font-Dateien — in res/font/ sind committed (Nunito + DM Mono von Google Fonts)
# Andere .ttf/.otf im Repo-Root oder Build-Output ausschließen
/build/**/*.ttf
/build/**/*.otf
